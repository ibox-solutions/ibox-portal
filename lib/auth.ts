/**
 * DEPRECATED — do not use this file.
 * AuthOptions are defined in: app/api/auth/[...nextauth]/route.ts
 * Import from there directly via:
 *   import { authOptions } from "@/app/api/auth/[...nextauth]/route"
 */
export {}
